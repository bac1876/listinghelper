# Storage Backend Configuration
# ==============================
# The app now supports both Bunny.net (recommended) and ImageKit
# Configure one or both - Bunny.net will be preferred if both are configured

# Bunny.net Configuration (RECOMMENDED - No video transformation limits!)
# ------------------------------------------------------------------------
# Sign up at https://bunny.net and create a Storage Zone
# Then create a Pull Zone connected to your Storage Zone
BUNNY_STORAGE_ZONE_NAME=your-storage-zone-name
BUNNY_ACCESS_KEY=your-storage-zone-password
BUNNY_PULL_ZONE_URL=https://yourzone.b-cdn.net
BUNNY_REGION=  # Leave empty for main region

# ImageKit Configuration (FALLBACK - Has video transformation limits)
# --------------------------------------------------------------------
# Only needed if you want to keep using ImageKit as a fallback
IMAGEKIT_PRIVATE_KEY=your_private_key_here
IMAGEKIT_PUBLIC_KEY=your_public_key_here
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_imagekit_id

# GitHub Actions Configuration (Required for high-quality video rendering)
# Set up GitHub Actions workflow in your repo
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_OWNER=your_github_username
GITHUB_REPO=your_repo_name
USE_GITHUB_ACTIONS=true  # Set to true to enable GitHub Actions rendering
GITHUB_WEBHOOK_SECRET=your_webhook_secret  # Optional but recommended

# Flask Configuration
FLASK_ENV=production
PORT=5000